TASK: Phase 1 — Task 16: Advanced Chart Inspector (Hybrid Inspector)

═══════════════════════════════════════════════════════════════
OBJECTIVE
═══════════════════════════════════════════════════════════════
Build the Advanced Chart Inspector for the Analytics Workspace. This component will live in the right sidebar (Inspector pane) and provide a two-tab interface (`Data` and `Format`) for deep ECharts customization, plus a raw JSON override block for power users.

Spec (READ ONLY — implement from it, never edit):
  docs/specs/phase-1/06_bi_pivot_spec.md

═══════════════════════════════════════════════════════════════
CONSTRAINTS & RULES
═══════════════════════════════════════════════════════════════
- The Inspector MUST react to the global `workspaceStore.inspectorState`.
- Svelte 5 Runes: Use `$state` and `$derived` for all reactive properties.
- JSON Parsing: The "Raw JSON Override" feature must use a `try/catch` around `JSON.parse()`. If parsing fails, do NOT crash the app; simply show a small warning indicator and do not apply the invalid config.

═══════════════════════════════════════════════════════════════
CONTEXT — EXISTING REPO LAYOUT & ARCHITECTURE
═══════════════════════════════════════════════════════════════
- `src/lib/components/inspector/` (Target directory for the new components)
- `src/lib/utils/chartBuilder.ts` (The underlying chart builder logic that you will need to intercept or extend with the overrides)

═══════════════════════════════════════════════════════════════
IMPLEMENTATION TIPS
═══════════════════════════════════════════════════════════════
- Tabs Component: Re-use the existing Tabs component from `src/lib/components/ui/tabs/` if available, or build a simple accessible tab list.
- Deep Merge: For the JSON override, use a deep merge utility (or lodash `merge` if available) to combine the user's JSON with the base config generated by `chartBuilder.ts`.
- Color Picker: Use a native `<input type="color">` for the individual series color overrides.

═══════════════════════════════════════════════════════════════
DELIVERABLES
═══════════════════════════════════════════════════════════════
1. NEW: `src/lib/components/inspector/ChartInspector.svelte`
2. MODIFY: `src/lib/components/workspace/panels/AnalyticsWorkspace.svelte` (to wire up the inspector state when a chart is clicked)
3. MODIFY: `src/lib/utils/chartBuilder.ts` (to apply overrides)

Commit: "feat: Phase 1 Task 16 Advanced Chart Inspector"
Target branch: feature/task16-chart-inspector
